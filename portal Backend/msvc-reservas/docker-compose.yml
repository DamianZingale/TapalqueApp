version: "3.9"

services:
  msvc-reservas:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8002:8003"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_DATA_MONGODB_URI=mongodb://root:root@mongodb:27017/reservasdb
    depends_on:
      - mongodb
  mongodb:
    image: mongo 7.0
    container_name: mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root 
      MONGO_INITDB_ROOT_PASSWORD: root
      MONGO_INITDB_DATABASE: reservasdb
    volumes:
      - mongo-data:/data/db
    restart: always
volumes:
  mongo-data: